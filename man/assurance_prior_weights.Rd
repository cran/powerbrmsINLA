% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute-assurance.R
\name{assurance_prior_weights}
\alias{assurance_prior_weights}
\title{Create prior weights over an effect grid for use with compute_assurance()}
\usage{
assurance_prior_weights(effects, dist = c("normal", "uniform", "beta"), ...)
}
\arguments{
\item{effects}{Numeric vector of effect-grid values (the same vector
passed to \code{\link[=brms_inla_power]{brms_inla_power()}} as \code{effect_grid}).}

\item{dist}{Character string naming the distribution.  One of
\code{"normal"}, \code{"uniform"}, or \code{"beta"}.}

\item{...}{Named distribution parameters forwarded to the internal
density calculator.  See \code{\link[=compute_assurance]{compute_assurance()}} \verb{@details} for the
full list per distribution.}
}
\value{
Named numeric vector of normalised weights (sums to 1), with
names equal to \code{as.character(effects)}.
}
\description{
A convenience wrapper that evaluates a parametric prior distribution at
each value in \code{effects}, normalises the densities to sum to 1, and returns
a named numeric vector that can be passed directly to \code{\link[=compute_assurance]{compute_assurance()}}
as \code{prior_weights}.
}
\details{
The output format is identical to that of \code{\link[=beta_weights_on_grid]{beta_weights_on_grid()}}, which
can also be used directly when a beta prior is appropriate.
}
\examples{
effects <- c(0.1, 0.3, 0.5, 0.7, 0.9)
# Normal prior centred on 0.5
assurance_prior_weights(effects, dist = "normal", mean = 0.5, sd = 0.2)
# Uniform prior (equal weight)
assurance_prior_weights(effects, dist = "uniform")
}
