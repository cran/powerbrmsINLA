% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{validate_inla_vs_brms}
\alias{validate_inla_vs_brms}
\title{Spot-check INLA posterior estimates against brms/Stan}
\usage{
validate_inla_vs_brms(
  formula,
  family = gaussian(),
  priors = NULL,
  data_generator = NULL,
  effect_name,
  effect_value = 0.5,
  sample_size = 100L,
  n_check = 5L,
  brms_iter = 2000L,
  brms_chains = 4L,
  tolerance = NULL,
  seed = 42L,
  error_sd = 1,
  group_sd = 0.5,
  obs_per_group = 10,
  predictor_means = NULL,
  predictor_sds = NULL,
  family_args = list(),
  inla_num_threads = NULL
)
}
\arguments{
\item{formula}{Model formula (same as \code{\link[=brms_inla_power]{brms_inla_power()}}).}

\item{family}{GLM family object (default \code{gaussian()}).}

\item{priors}{A \code{brms::prior()} specification or \code{NULL} for defaults.
Supported priors are translated to INLA controls where possible and audited
in the returned settings.}

\item{data_generator}{Optional function \code{f(n, effect)} returning a
data frame.  If \code{NULL}, the automatic generator is used.}

\item{effect_name}{Character vector of fixed-effect names (same as
\code{\link[=brms_inla_power]{brms_inla_power()}}).}

\item{effect_value}{Numeric value (or named vector) of the true effect
size used when generating data for the comparison.  Default \code{0.5}.}

\item{sample_size}{Integer sample size for each comparison dataset.
Default \code{100}.}

\item{n_check}{Number of independent datasets to compare.  Default \code{5}.}

\item{brms_iter}{Total MCMC iterations per chain passed to
\code{brms::brm()}.  Default \code{2000}.}

\item{brms_chains}{Number of MCMC chains.  Default \code{4}.}

\item{tolerance}{Numeric threshold: the flag is set to \code{TRUE} when the
maximum absolute difference in posterior means exceeds \code{tolerance}.
If \code{NULL} (default), \verb{0.1 * mean(INLA posterior SD)} is used.}

\item{seed}{Integer random seed.  Default \code{42}.}

\item{error_sd, group_sd, obs_per_group, predictor_means, predictor_sds, family_args}{Passed to the automatic data generator when \code{data_generator = NULL}
(see \code{\link[=brms_inla_power]{brms_inla_power()}} for details).}

\item{inla_num_threads}{INLA threading string (e.g. \code{"4:1"}).  Auto-
detected when \code{NULL}.}
}
\value{
A list with components:
\describe{
\item{\code{comparisons}}{Data frame with one row per dataset and columns
\code{sim}, \code{inla_ok}, \code{brms_ok}, \code{inla_mean}, \code{brms_mean}, \code{diff_mean},
\code{inla_sd}, \code{brms_sd}, \code{inla_ci_lower}, \code{inla_ci_upper},
\code{brms_ci_lower}, \code{brms_ci_upper}.}
\item{\code{flag}}{\code{TRUE} if any \verb{|diff_mean|} exceeds \code{tolerance}.}
\item{\code{max_abs_diff}}{Maximum observed \verb{|diff_mean|}.}
\item{\code{tolerance}}{The tolerance value actually used.}
\item{\code{settings}}{List of key settings for reproducibility.}
}
}
\description{
Generates a small number of synthetic datasets using the same internal
data-generator as \code{\link[=brms_inla_power]{brms_inla_power()}}, fits each dataset with both INLA
(as the package does) and \code{brms::brm()} (via Stan), and compares the
posterior mean, posterior SD, and 95 \% credible-interval bounds for the
primary effect parameter.

This is a \strong{qualitative spot-check}, not a formal equivalence test.
Differences are expected due to the Laplace approximation used by INLA
versus full MCMC in Stan, and will vary across models and priors.
}
\section{Performance warning}{

This function runs \code{n_check} full brms/Stan fits.  Even with
\code{brms_chains = 2} and \code{brms_iter = 1000}, this can take several minutes
for non-trivial models.  Use \code{n_check = 3} and \code{brms_chains = 2} for
quick sanity checks.
}

