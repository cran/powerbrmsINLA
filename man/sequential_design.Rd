% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sequential-analysis.R
\name{sequential_design}
\alias{sequential_design}
\title{Prespecify a sequential Bayesian analysis design}
\usage{
sequential_design(
  formula,
  family = gaussian(),
  priors = NULL,
  effect_name,
  metric = c("direction", "threshold", "rope"),
  alternative = c("greater", "less"),
  looks,
  prob_success = 0.95,
  prob_futility = NULL,
  effect_threshold = 0,
  rope_bounds = NULL,
  credible_level = 0.95,
  label = NULL
)
}
\arguments{
\item{formula}{Model formula (brms syntax; random effects supported).}

\item{family}{Response family, e.g. \code{gaussian()}, \code{binomial()}.}

\item{priors}{Optional brms prior specification (translated to INLA via
the package's prior bridge; see \code{\link[=brms_inla_power]{brms_inla_power()}}).}

\item{effect_name}{Single character: the monitored fixed effect.}

\item{metric}{\code{"direction"}, \code{"threshold"}, or \code{"rope"}.}

\item{alternative}{\code{"greater"} or \code{"less"}: the prespecified direction for
the direction/threshold metrics. Ignored for \code{"rope"}.}

\item{looks}{Strictly increasing vector of cumulative sample sizes (total
observations) at which interim analyses are planned. The final element
is the maximum sample size.}

\item{prob_success}{Scalar or \code{length(looks)} vector of stop-for-success
posterior probability thresholds in (0.5, 1). Per-look vectors allow
stricter early thresholds (O'Brien-Fleming-like behaviour).}

\item{prob_futility}{Scalar or \code{length(looks)} vector of stop-for-futility
thresholds in (0, 0.5), or \code{NULL} (default) for no futility stopping.
Ignored with a warning for \code{metric = "rope"}.}

\item{effect_threshold}{Threshold for \code{metric = "threshold"} (model
coefficient scale).}

\item{rope_bounds}{Length-2 increasing numeric vector for
\code{metric = "rope"}.}

\item{credible_level}{Credible level for reported intervals (default 0.95).}

\item{label}{Optional character label for the study (used in printing).}
}
\value{
A list of class \code{"powerbrmsINLA_seq_design"} whose \code{fingerprint}
element contains the MD5 hash of the decision-relevant fields.
}
\description{
Creates a frozen specification of a sequential Bayesian analysis
\emph{before} data collection: the model, the analysis priors, the
monitored effect, the decision metric, the stopping thresholds, and the
planned look schedule. The returned object is used both to simulate the
design's operating characteristics (\code{\link[=brms_inla_sequential_trial]{brms_inla_sequential_trial()}}) and to
monitor the real study as data accumulate (\code{\link[=sequential_analysis]{sequential_analysis()}}).

The object carries an MD5 fingerprint of all decision-relevant fields.
Quoting this fingerprint in a preregistration or protocol allows readers
and reviewers to verify that the stopping rules were not altered after
data collection began: any change to the model, priors, metric,
thresholds, or look schedule produces a different fingerprint.
}
\details{
Because Bayesian posterior quantities obey the likelihood principle, the
interpretation of the posterior at the final look does not depend on the
stopping rule. The \emph{frequency} properties of the design (false-go
rate, expected sample size, exaggeration of estimates at early stops) do.
The recommended workflow is therefore: (1) create the design; (2) estimate
its operating characteristics by simulation with
\code{\link[=brms_inla_sequential_trial]{brms_inla_sequential_trial()}}; (3) preregister the design, including its
fingerprint; (4) monitor the study with \code{\link[=sequential_analysis]{sequential_analysis()}}; and
(5) report the final posterior alongside the simulated operating
characteristics.
}
\examples{
design <- sequential_design(
  formula          = strength ~ group,
  effect_name      = "group",
  metric           = "threshold",
  effect_threshold = 0.2,
  looks            = c(40, 80, 120, 160, 200),
  prob_success     = 0.95,
  prob_futility    = 0.05,
  label            = "Periodisation RCT"
)
print(design)

}
\seealso{
\code{\link[=sequential_analysis]{sequential_analysis()}}, \code{\link[=brms_inla_sequential_trial]{brms_inla_sequential_trial()}},
\code{\link[=plot_sequential_monitor]{plot_sequential_monitor()}}
}
