% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting-assurance.R
\name{plot_design_prior}
\alias{plot_design_prior}
\title{Plot Design Prior Density over the Effect Grid}
\usage{
plot_design_prior(
  prior_spec,
  effect_grid,
  labels = NULL,
  n_points = 300L,
  title = NULL,
  subtitle = NULL
)
}
\arguments{
\item{prior_spec}{A single prior specification list (e.g.
\code{list(dist = "normal", mean = 0.5, sd = 0.15)}) \strong{or} a list of such
lists.  Supported distributions: \code{"normal"} (\code{mean}, \code{sd}),
\code{"uniform"} (\code{min}, \code{max}), \code{"beta"} (\code{shape1}, \code{shape2} or \code{mode}, \code{n}).}

\item{effect_grid}{Numeric vector of effect-grid values passed to
\code{\link[=brms_inla_power]{brms_inla_power()}}.  Vertical dashed lines are drawn at each value.}

\item{labels}{Character vector naming each prior.  Defaults to
\code{"Design prior"} for a single spec or \code{"Prior 1"}, \code{"Prior 2"}, … for
multiple.}

\item{n_points}{Number of x points at which to evaluate the density curve
(default 300).}

\item{title, subtitle}{Optional plot title and subtitle strings.}
}
\value{
A ggplot object.
}
\description{
Visualises one or more design prior distributions over the effect-size range
used in a power analysis.  Vertical dashed lines mark the grid points at
which power was simulated, helping assess prior-grid alignment.
}
\examples{
plot_design_prior(
  prior_spec  = list(dist = "normal", mean = 0.5, sd = 0.15),
  effect_grid = c(0.2, 0.5, 0.8)
)

# Multiple priors
plot_design_prior(
  prior_spec  = list(
    list(dist = "normal", mean = 0.5, sd = 0.10),
    list(dist = "normal", mean = 0.5, sd = 0.30)
  ),
  effect_grid = c(0.2, 0.5, 0.8),
  labels      = c("Informative", "Diffuse")
)

}
