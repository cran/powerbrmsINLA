% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine-sequential-trial.R
\name{brms_inla_sequential_trial}
\alias{brms_inla_sequential_trial}
\title{Simulate a sequential Bayesian trial with interim stopping rules}
\usage{
brms_inla_sequential_trial(
  design = NULL,
  formula = NULL,
  family = gaussian(),
  family_control = NULL,
  priors = NULL,
  data_generator = NULL,
  effect_name = NULL,
  true_effect = 0.5,
  looks = NULL,
  nsims = 200,
  metric = c("direction", "threshold", "rope"),
  alternative = c("greater", "less"),
  prob_success = 0.95,
  prob_futility = NULL,
  effect_threshold = 0,
  rope_bounds = NULL,
  credible_level = 0.95,
  error_sd = 1,
  group_sd = 0.5,
  obs_per_group = 10,
  predictor_means = NULL,
  predictor_sds = NULL,
  seed = 123,
  inla_num_threads = NULL,
  family_args = list(),
  progress = c("auto", "text", "none")
)
}
\arguments{
\item{design}{Optional \code{"powerbrmsINLA_seq_design"} object from
\code{\link[=sequential_design]{sequential_design()}}. When supplied, it overrides \code{formula}, \code{family},
\code{priors}, \code{effect_name}, \code{metric}, \code{alternative}, \code{looks},
\code{prob_success}, \code{prob_futility}, \code{effect_threshold}, \code{rope_bounds}, and
\code{credible_level}, so the simulated operating characteristics correspond
exactly to the design that will be monitored with
\code{\link[=sequential_analysis]{sequential_analysis()}}.}

\item{formula}{Model formula (brms syntax; random effects supported).}

\item{family}{Response family, e.g. \code{gaussian()}, \code{binomial()}.}

\item{family_control}{Optional INLA \code{control.family} list; takes precedence
over translated priors, as in \code{\link[=brms_inla_power]{brms_inla_power()}}.}

\item{priors}{Optional brms prior specification, translated via the
built-in prior bridge (see \code{\link[=brms_inla_power]{brms_inla_power()}}).}

\item{data_generator}{Optional \verb{function(n, effect)} returning a data frame
with \code{n} rows \strong{ordered by accrual time} (see Details). Defaults to the
package's automatic generator.}

\item{effect_name}{Single character: the primary (monitored) effect.}

\item{true_effect}{Either a numeric vector of fixed true-effect scenarios
(conditional sequential power per value), or a design-prior list such as
\code{list(dist = "normal", mean = 0.5, sd = 0.15)}, in which case a fresh
true effect is drawn for every simulated trial and the resulting success
proportion is the design's \emph{sequential assurance}.}

\item{looks}{Increasing integer vector of cumulative sample sizes (total
observations, the same units as \code{sample_sizes} in \code{\link[=brms_inla_power]{brms_inla_power()}})
at which interim analyses occur. The last element is the maximum n.}

\item{nsims}{Number of simulated trials per scenario.}

\item{metric}{\code{"direction"}, \code{"threshold"}, or \code{"rope"}.}

\item{alternative}{\code{"greater"} or \code{"less"}; the prespecified direction for
the direction/threshold metrics. Ignored for \code{"rope"}.}

\item{prob_success}{Scalar or \code{length(looks)} vector of stop-for-success
posterior probability thresholds (default 0.95).}

\item{prob_futility}{Scalar or \code{length(looks)} vector of stop-for-futility
thresholds, or \code{NULL} (default) to disable futility stopping. Ignored
with a warning for \code{metric = "rope"}.}

\item{effect_threshold}{Threshold for \code{metric = "threshold"} (on the model
coefficient scale; use a negative value with \code{alternative = "less"}).}

\item{rope_bounds}{Length-2 numeric vector for \code{metric = "rope"}.}

\item{credible_level}{Credible level used for the reported interval widths.}

\item{error_sd, group_sd}{Scalar or distributional list, as in
\code{\link[=brms_inla_power]{brms_inla_power()}}. With a distributional specification a fresh value is
drawn once per simulated \emph{trial} (a trial has one true nuisance value;
it does not change between looks).}

\item{obs_per_group, predictor_means, predictor_sds}{Passed to the automatic
data generator (see \code{\link[=brms_inla_power]{brms_inla_power()}}).}

\item{seed}{Integer seed.}

\item{inla_num_threads}{INLA threads ("outer:inner"); auto-detected if NULL.}

\item{family_args}{Named list of family-specific arguments for the
automatic generator.}

\item{progress}{\code{"auto"}, \code{"text"}, or \code{"none"}.}
}
\value{
A list of class \code{"powerbrmsINLA_seq_trial"} with components:
\itemize{
\item \code{results}: one row per simulated trial (scenario, true effect,
decision, stopping n and look, posterior estimate at stopping).
\item \code{summary}: operating characteristics per scenario (see Details).
\item \code{look_summary}: per-look stopping distribution per scenario.
\item \code{diagnostics}: failed/warned INLA fits per scenario.
\item \code{settings}: a record of all design parameters.
}
}
\description{
Simulates trials in which data \strong{accumulate} and are analysed with INLA at
a prespecified schedule of interim looks. At each look a posterior
probability criterion is evaluated and the trial stops for success,
futility, or (for the ROPE metric) practical equivalence; otherwise it
continues to the maximum sample size. The function returns the design's
operating characteristics: the probability of each decision, the
distribution of stopping times, the expected sample size, and the average
effect estimate at early success stops (a direct measure of optional
stopping exaggeration).

This complements \code{\link[=brms_inla_power]{brms_inla_power()}} (fixed-n designs) and is \strong{not} the
same as \code{\link[=brms_inla_power_sequential]{brms_inla_power_sequential()}}, which adaptively stops the Monte
Carlo simulation itself rather than simulating sequential trials.

Because Bayesian posterior quantities obey the likelihood principle, the
\emph{interpretation} of the final posterior is unaffected by the stopping rule;
the purpose of this simulation is to quantify the \emph{frequency} properties of
the design (false-go rate, expected sample size, estimate exaggeration),
which do depend on the stopping rule.
}
\details{
\strong{Accrual model.} For each simulated trial one dataset of the maximum size
is generated, and the analysis at a look of size \code{n} uses its first \code{n}
rows, so the data genuinely accumulate across looks. With the automatic
data generator, rows are exchangeable given the random effects (grouping
factors cycle across rows), which corresponds to all clusters recruiting in
parallel while observations accumulate within them. For a different accrual
pattern (for example, whole clusters arriving one at a time), supply a
custom \code{data_generator} whose rows are ordered by accrual time.

\strong{Decision rules.} Let \code{pr} be the posterior probability of the
prespecified alternative at a look (computed, as elsewhere in the package,
from a Normal approximation to the INLA fixed-effect posterior):
\itemize{
\item \code{metric = "direction"}: \code{pr = P(effect > 0 | data)} (or \verb{< 0} when
\code{alternative = "less"}).
\item \code{metric = "threshold"}: \code{pr = P(effect > effect_threshold | data)}
(or \code{<} when \code{alternative = "less"}).
\item \code{metric = "rope"}: two probabilities are monitored; the trial stops
for "success" when \verb{P(outside ROPE) >= prob_success} and for
"equivalence" when \verb{P(inside ROPE) >= prob_success}.
}
For the direction and threshold metrics the trial stops for success when
\code{pr >= prob_success} and for futility when \code{pr <= prob_futility} (futility
stopping is disabled when \code{prob_futility = NULL}). \code{prob_success} and
\code{prob_futility} may be vectors of \code{length(looks)} to implement stricter
early thresholds (O'Brien-Fleming-like behaviour).

\strong{What to report.} \code{summary} contains, per scenario: \code{p_success},
\code{p_futility}, \code{p_equivalence}, \code{p_inconclusive}, \code{expected_n},
\code{p_stop_early}, and \code{mean_est_at_success} next to \code{mean_true_at_success}
(their difference estimates the exaggeration of effect estimates caused by
early stopping). Under \code{true_effect} containing 0, \code{p_success} is the
false-go rate of the design.
}
\examples{
\dontrun{
looks <- c(40, 80, 120, 160, 200)

# Conditional sequential power at fixed true effects (0 = false-go rate)
seq_fixed <- brms_inla_sequential_trial(
  formula          = y ~ treatment,
  effect_name      = "treatment",
  true_effect      = c(0, 0.5),
  looks            = looks,
  nsims            = 200,
  metric           = "threshold",
  effect_threshold = 0.2,
  prob_success     = 0.95,
  prob_futility    = 0.05,
  seed             = 123
)
print(seq_fixed)

# Sequential assurance: draw the true effect from a design prior
seq_assur <- brms_inla_sequential_trial(
  formula          = y ~ treatment,
  effect_name      = "treatment",
  true_effect      = list(dist = "normal", mean = 0.5, sd = 0.15),
  looks            = looks,
  nsims            = 200,
  metric           = "threshold",
  effect_threshold = 0.2,
  prob_futility    = 0.05,
  seed             = 123
)
print(seq_assur)
}

}
