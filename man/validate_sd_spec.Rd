% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-helpers.R
\name{validate_sd_spec}
\alias{validate_sd_spec}
\title{Validate an SD Specification for error_sd or group_sd}
\usage{
validate_sd_spec(x, arg_name = "x")
}
\arguments{
\item{x}{A positive numeric scalar \strong{or} a named list with element \code{dist}.}

\item{arg_name}{Character string used in error messages (default \code{"x"}).}
}
\value{
\code{x}, invisibly.  Called for its side effects (stopping on invalid
input).
}
\description{
Checks whether the input is a valid positive numeric scalar or one of the
supported distributional list specifications.  Called automatically by
\code{\link[=brms_inla_power]{brms_inla_power()}} before the simulation loop; can also be called
directly for interactive validation.
}
\details{
Supported distributional formats:
\itemize{
\item \code{list(dist = "halfnormal", sd = X, location = Y)} — draws
\verb{|Normal(location, sd)|}; \code{location} defaults to 0.
\item \code{list(dist = "lognormal", meanlog = X, sdlog = Y)} — draws from a
log-normal distribution.
\item \code{list(dist = "uniform", min = X, max = Y)} — draws from Uniform(min, max);
requires \code{min >= 0}.
}
}
