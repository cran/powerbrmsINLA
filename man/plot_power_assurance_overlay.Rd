% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting-assurance.R
\name{plot_power_assurance_overlay}
\alias{plot_power_assurance_overlay}
\title{Plot Conditional Power Curves with Assurance Overlay}
\usage{
plot_power_assurance_overlay(
  power_result,
  assurance_result,
  metric = c("direction", "threshold", "rope", "bf"),
  title = NULL,
  subtitle = NULL
)
}
\arguments{
\item{power_result}{Output from \code{\link[=brms_inla_power]{brms_inla_power()}} (or compatible wrappers)
containing a \verb{$summary} element.}

\item{assurance_result}{A \code{powerbrmsINLA_assurance} object from
\code{\link[=compute_assurance]{compute_assurance()}}.}

\item{metric}{Decision metric to plot: \code{"direction"} (default),
\code{"threshold"}, \code{"rope"}, or \code{"bf"}.}

\item{title, subtitle}{Optional plot title and subtitle strings.}
}
\value{
A ggplot object.
}
\description{
Shows the conditional power curve for each effect-size value in the design
grid (thin lines), overlaid with the unconditional Bayesian assurance curve
(thick line).  This communicates that assurance is the prior-weighted
average of the conditional power curves.
}
\examples{
syn_summary <- data.frame(
  n               = rep(c(50, 100, 200), each = 3),
  treatment       = rep(c(0.2, 0.5, 0.8), 3),
  power_direction = c(0.40, 0.65, 0.85, 0.60, 0.82, 0.95, 0.72, 0.90, 0.98)
)
pr <- list(summary = syn_summary, settings = list(effect_name = "treatment"))
a  <- compute_assurance(pr, list(dist = "normal", mean = 0.5, sd = 0.15))
plot_power_assurance_overlay(pr, a)

}
