% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute-assurance.R
\name{.compute_weights_from_dist}
\alias{.compute_weights_from_dist}
\title{Compute weights from a parametric prior distribution over an effect grid}
\usage{
.compute_weights_from_dist(dist_spec, effect_values)
}
\arguments{
\item{dist_spec}{A list with at minimum a \verb{$dist} element naming the
distribution.  Remaining elements are distribution parameters:
\itemize{
\item \code{"normal"} — \code{mean}, \code{sd}
\item \code{"uniform"} — \code{min}, \code{max} (defaults: range of \code{effect_values})
\item \code{"beta"} — either \code{shape1}, \code{shape2} \emph{or} \code{mode}, \code{n} (concentration;
must be > 2).  Effects are rescaled to \eqn{[0, 1]} automatically when
outside that interval.
}}

\item{effect_values}{Numeric vector of effect-grid values at which to
evaluate the density.}
}
\value{
Named numeric vector of normalised weights (sums to 1).
}
\description{
Evaluates a named distribution at each value in \code{effect_values}, normalises
the result to sum to 1, and returns a named numeric vector compatible with
\code{\link[=compute_assurance]{compute_assurance()}} and \code{\link[=beta_weights_on_grid]{beta_weights_on_grid()}}.
}
\keyword{internal}
